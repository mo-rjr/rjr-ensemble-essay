package uk.gov.metoffice.hello.explode.impacts;

/**
 * {A thing} to {do something} for {another thing}
 * -- for example, {this}
 * -- and also {this}
 */
// TODO fill in Javadoc
public class StormStormImpactTypeLevelsProviderTest {

    private static final String ROOT = "C:\\Useful\\SWF_HIM\\SWF-Mashup\\SWFHIM\\NCENS_Live\\processing\\SupplementaryData\\ImpactLibrary\\NineScenario_v01\\";

//    @Test
//    public void getFor() {
//
//        // arrange
//        List<Integer> validBlocks = new ValidBlocksReader().readValidBlocks();
//        StormImpactLevelsProvider testObject = new StormImpactLevelsProvider(ROOT, validBlocks);
//
//        // act
//        StormImpactLevels result = testObject.getFor(StormDuration.ONE_HOUR, StormReturnPeriod.THIRTY_YEARS);
//
//
//        // assert
//        assertNotNull(result);
//        assertEquals(5, result.getImpactLevels().size());
//        assertTrue(result.getImpactLevels().values().stream().allMatch(x -> x != null && x.size() > 0));
//
//    }
}